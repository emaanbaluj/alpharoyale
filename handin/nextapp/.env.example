# Frontend Environment Variables
# For local development: cp .env.example .env.local
# For production: Set these in your hosting platform (Vercel/Cloudflare Pages)

# Local Supabase (get these from `supabase start` output)
# For production: Use your Supabase project URL and anon key
NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your_anon_key_from_supabase_start

# Finnhub API (not used in frontend currently, but available if needed)
# FINNHUB_API_KEY=your_finnhub_api_key_here